# Optional — copy to .env.local for local Flask backend (not committed).
# Only use when deployment is "auto" or "selfhost" in deployment.config.json.
# Leave unset when testing official prod UI against Railway.
# REACT_APP_API_URL=http://localhost:5000
