[phases.setup]
nixPkgs = ["libreoffice-bin", "python311"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "export PATH=$PATH:/nix/store/*-libreoffice-*/bin && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 150"